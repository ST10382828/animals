package student.projects.animalsindistress.ui.theme

import androidx.compose.ui.graphics.Color

// Colors matching React site - hsl(220, 100%, 25%)
val Primary = Color(0xFF003D99) // Dark blue primary
val PrimaryGlow = Color(0xFF0052CC) // Lighter blue for hover/active states
val PrimaryForeground = Color(0xFFFFFFFF) // White text on primary

// Secondary colors - hsl(220, 60%, 50%)
val Secondary = Color(0xFF3366CC)
val SecondaryForeground = Color(0xFFFFFFFF)

// Accent colors - hsl(220, 80%, 40%)
val Accent = Color(0xFF1A5CCC)
val AccentForeground = Color(0xFFFFFFFF)

// Background and Surface
val Background = Color(0xFFFFFFFF) // White
val Foreground = Color(0xFF2D3748) // Dark gray for text

// Muted colors
val Muted = Color(0xFFF2F4F7) // Light gray background
val MutedForeground = Color(0xFF6B7280) // Gray text

// Card colors
val Card = Color(0xFFFFFFFF)
val CardForeground = Color(0xFF2D3748)

// Border and Input
val Border = Color(0xFFE5E7EB)
val Input = Color(0xFFE5E7EB)

// Success, Warning, Destructive
val Success = Color(0xFF10B981) // Green
val SuccessForeground = Color(0xFFFFFFFF)
val Warning = Color(0xFFF59E0B) // Orange
val WarningForeground = Color(0xFF2D3748)
val Destructive = Color(0xFFEF4444) // Red
val DestructiveForeground = Color(0xFFFFFFFF)